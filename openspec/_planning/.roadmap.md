# Specfly — roadmap

Brief list of next changes, in rough order. Detail each one when we start it.
History of shipped work: `archive/STATUS.md`.

- [x] R7 — Fix apply-run logging in the Github Action, port the fixes from `remcc`, log the same way as there.
- [x] R8 — Change trigger prefix `@spec:apply` → `/spec:apply` (`/spec:<command>` is the convention going forward).
- [ ] R9 — Claude Code skill to apply a change remotely: one command crafts the trigger commit + pushes, no manual commit/message/push.
- [ ] R1 — Migrate domain `specfly.io` → `specfly.dev`.
- [ ] R2 — Branding + promotion — logo, README, `specfly.dev` landing page.

Smaller / when-ready:

- [ ] R3 — `cd backend && wrangler deploy` to ship the PKCS#8 guard (PR #4) — safety net only.
- [ ] R4 — Make the GitHub App public ("Any account") for outside adopters.
- [ ] R5 — Review/merge adopter PR #18 (`fix-github-org-link`).
- [ ] R6 — Delete stale merged branches: `harden-private-key-pkcs8`, `setup/specfly-caller`,
      local `harden-apply-pnpm-corepack`, `harden-apply-bun-script`.
