# Specfly — roadmap

Brief list of next changes, in rough order. Detail each one when we start it.
History of shipped work: `archive/STATUS.md`.

- [x] R7 — Fix apply-run logging in the Github Action, port the fixes from `remcc`, log the same way as there. → `archive/2026-05-27-enrich-apply-run-logging`
- [x] R8 — Change trigger prefix `@spec:apply` → `/spec:apply` (`/spec:<command>` is the convention going forward). → `archive/2026-05-27-slash-spec-trigger-prefix`
- [x] R10 — Make `apply.yml` technology-agnostic: drop pnpm/bun provisioning, let the agent provision its own environment so `/spec:apply` runs on any stack. → `archive/2026-05-28-tech-agnostic-apply`
- [x] R9 — Claude Code skill to apply a change remotely: one command crafts the trigger commit + pushes, no manual commit/message/push.
- [x] R1 — Migrate domain `specfly.io` → `specfly.dev`.
- [x] R11 - Rename specfly command(s) pattern from `/spec:[command]` to `/sfx:[command]` to lower risk of conflicts with other toolings
- [x] R3 — `cd backend && wrangler deploy` to ship the PKCS#8 guard (PR #4) — safety net only.
- [x] R5 — Review/merge adopter PR #18 (`fix-github-org-link`).
- [x] R6 — Delete stale merged branches: `harden-private-key-pkcs8`, `setup/specfly-caller`,
      local `harden-apply-pnpm-corepack`, `harden-apply-bun-script`.
- [x] R13 - shell installation script that will minimize the installation steps and guide the user step by step in doing the steps that cannot be reasonably easily automated
- [x] R2 — Branding + promotion — crisp README.md landing page content and other docs; key benefits: safe unattended runs in ephemeral isolated environments, cost control, composability into bigger workflows
- [ ] R12 - content for and Cloudflare deployment of a lean, static public landing page `specfly.dev` based on the content of README.md and other documentation; do not overload it, just give the main highlights based on existing docs, nice sleek design with emerald-green accent and emphasize the curl install script. Make sure the install script works.
- [ ] R4 — Make the GitHub App listed in public (complete the listing forms).
